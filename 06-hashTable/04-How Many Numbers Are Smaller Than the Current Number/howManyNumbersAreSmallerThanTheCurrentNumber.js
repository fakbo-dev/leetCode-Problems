// Given the array nums, for each nums[i] find out how many numbers in the array are smaller than it. That is, for each nums[i] you have to count the number of valid j's such that j != i and nums[j] < nums[i].

// Return the answer in an array.

// Step

// 1. dado un array de numeros "nums", por cada "nums[i]" encuentra cuantos numeros en el array son mas pequenos que el, por cada "nums[i]" necesitas contar el numero de j validas tal que j !== i y "nums[j] < nums[i]".

// 2. retorna la respuesta en un array
const smallerNumbersThanCurrent = (nums) => {


};

smallerNumbersThanCurrent([8, 1, 2, 2, 3]) //Expected Ouput = [4,0,1,1,3]